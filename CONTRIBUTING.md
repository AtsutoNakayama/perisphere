# コントリビューションガイド

perisphere への貢献に興味を持っていただきありがとうございます。
このドキュメントは、ブランチ運用・Issue/PR の流れ・コミット規約をまとめたものです。

## 行動規範

すべての参加者は本プロジェクトの行動規範に従ってください（`CODE_OF_CONDUCT.md` を参照、整備中）。

## はじめに（前提）

- 本プロジェクトは **GitHub Flow**（`main` を常にリリース可能な状態に保つトランクベース運用）を採用します。
- `main` への直接 push は禁止です。すべての変更は **Pull Request（PR）経由**でマージします。
- 重要な変更は **Issue を起点**に進めます（後述）。

## コントリビューションの流れ

### 外部コントリビューターの方

1. このリポジトリを **Fork** する
2. Fork 側で作業ブランチを切る
3. 変更をコミットする
4. 元リポジトリへ **Pull Request** を送る

### コアメンバー（リポジトリへの書き込み権限がある人）

Fork は不要です。リポジトリ内で直接作業ブランチを切って PR を送ってください。

## Issue ベース運用

| 変更の種類 | Issue の扱い |
|---|---|
| 機能追加・仕様変更・バグ修正 | **原則 Issue を起票してから着手**（議論・合意・重複防止のため） |
| typo 修正・ドキュメントの軽微な修正・小さなリファクタ | Issue なしで PR 直行を許容 |

着手前に Issue を確認し、重複や進行中の作業がないかをチェックしてください。

## ブランチ運用

- ブランチは **短命**に保ちます（数日〜1週間でマージ）。長生きさせると衝突の原因になります。
- ブランチ名の規約は以下のとおりです。

```
<type>/<issue番号>-<短い説明>
```

例:

```
docs/2-contributing
feat/12-add-config-loader
fix/34-handle-empty-input
```

Issue に紐づかない軽微な変更の場合は、`<type>/<短い説明>`（番号なし）も可とします。

`<type>` は次のコミット規約の type と揃えます。

## コミットメッセージ規約（Conventional Commits）

[Conventional Commits](https://www.conventionalcommits.org/) に従います。

```
<type>: <変更内容の簡潔な説明>

<本文（任意・なぜ/何を）>
```

利用する `type`:

| type | 用途 |
|---|---|
| `feat` | 新機能 |
| `fix` | バグ修正 |
| `docs` | ドキュメント |
| `chore` | 雑務・設定・整備 |
| `ci` | CI / ビルド |
| `refactor` | リファクタリング |
| `test` | テスト |

- 説明は「何をしたか」が分かるように書きます（`initial commit` のような内容ゼロの文言は避ける）。
- 日本語・英語どちらでも構いませんが、`type` は上記の英語キーワードを使います。

## Pull Request の作法

- PR 本文に関連 Issue を記載します。`Closes #123` と書くと、マージ時に Issue が自動でクローズされます。
- PR のタイトルも Conventional Commits の形式に揃えると、履歴が読みやすくなります。
- マージは **Squash and merge** を基本とし、`main` の履歴を「1 機能 = 1 コミット」に保ちます。
- CI（整備後）がグリーンであることを確認してください。

## ラベル

Issue / PR には種別を示す type ラベル（`feat` / `fix` / `docs` / `chore` / `ci` / `refactor` / `test`）と、
必要に応じて優先度ラベル（`P0` / `P1` / `P2`）を付与します。

## ライセンス

コントリビューションは、本リポジトリのライセンス（`LICENSE` を参照、整備中）のもとで公開されることに同意したものとみなされます。
